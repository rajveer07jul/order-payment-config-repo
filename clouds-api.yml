server:
  port: 8090

spring:
  application:
    name: clouds-api

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true

app:
  message: Hello from GitHub Config Repository
  owner: Rajveer